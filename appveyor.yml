skip_tags: true

matrix:
  fast_finish: true

os: Visual Studio 2015

environment:
  matrix:
    - PYTHON_ARCH: "x86_64"
      platform: x64
    - PYTHON_ARCH: "x86"
      platform: Win32

build_script:
  - move /y Makefile.vc Makefile
  - nmake
  - nmake memcheck


